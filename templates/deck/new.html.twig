{% extends 'base.html.twig' %}

{% block title %}New Deck !{% endblock %}

{% block body %}

    <div class="container mt-5">
        <h1>Build a new Deck !</h1>
    </div>

    <div class="container mt-5">
        {{ include('deck/_form.html.twig') }}
    </div>
        
    <div class="container mt-5">
        <a href="{{ path('app_member_show', {'id': member.id}) }}"><button class="btn btn-warning">Cancel</button></a>
    </div>

{% endblock %}
