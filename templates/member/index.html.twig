{% extends 'base.html.twig' %}

{% block title %}Hello MemberController!{% endblock %}

{% block body %}

<div class="container mt-5">
  <h1>Members list</h1>
</div>


{# <div class="container">
    <table class="table table-bordered table-hover">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
            </tr>
        </thead>
        <tbody>
            {% for member in members %}
            <tr>
                <td><a href="{{ path('app_member_show', {'id': member.id}) }}">{{member.Id}}</a></td>
                
                <td>

                <a href="{{ path('app_member_show', {'id': member.id}) }}">{{member.nom}}</a>

                </td>
            </tr>

            {% endfor %}
        </tbody>

        <br>
    </table>
</div> #}

{# {% for member in members %}
<div class="card" style="width:400px">
  {# <img class="card-img-top" src="img_avatar1.png" alt="Card image"> 
  <div class="card-body">
    <h4 class="card-title">{{member.nom}}</h4>
    <p class="card-text">[Member description]</p>
    <a href="#" class="btn btn-primary">Show </a>
  </div>
</div>
{% endfor %} #}

<div class="container mt-5">
  <div class="row row-cols-1 row-cols-md-3 g-4">
    {% for member in members %}
      <div class="col-sm-3">
        <div class="card h-100" style="max-width: 540px;">
          <div class="card-body">
            <h4 class="card-title">{{member.nom}}</h4>
            <p class="card-text">[Member description]</p>
            <a href="{{ path('app_member_show', {'id': member.id, 'member': member}) }}" class="btn btn-primary">Show profile</a>
          </div>
        </div>
      </div>
    {% endfor %}
  </div>
</div>

{% endblock %}
